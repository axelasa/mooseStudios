import 'package:flutter/material.dart';
import '../constants/colors.dart';

BoxDecoration headerDecorationMenu = BoxDecoration(
    borderRadius: BorderRadius.circular(95),
    gradient: const LinearGradient(
        colors: [
          Colors.transparent,
          AppColors.bgLight1
        ])
);